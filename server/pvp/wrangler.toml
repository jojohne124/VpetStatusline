name = "vpet-pvp"
main = "worker.js"
compatibility_date = "2024-09-01"

# KV：存戰鬥卡。部署前先建好 namespace 並把 id 填進來。
#   npx wrangler kv namespace create CARDS
# 把回傳的 id 貼到下方 id =
[[kv_namespaces]]
binding = "CARDS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
